# Desktop · online

Electron cloud app (`electron/cloud/`): opens the hosted ERP in a window.

Add desktop-online-only renderer helpers here (e.g. update UI, deep links).
Keep main-process code in `electron/cloud/`.
