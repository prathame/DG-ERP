/**
 * Desktop · online (Electron cloud wrapper).
 * Main process: `electron/cloud/`.
 * Renderer uses the shared web UI + `platforms/shared` API helpers.
 */
export const DESKTOP_ONLINE = 'cloud' as const;
